node_modules/
# Session state and the dedicated Chrome profile: credentials, never committed.
.auth/
# Run artefacts: reports, traces, screenshots.
output/
.state/
.npm-cache/
.sheet-cache/
# Tesseract's unpacked local language cache. The source model comes from npm.
eng.traineddata
# Raw Playwright Codegen sessions may contain copied text or example form data.
# Keep them local until they are reviewed and converted into maintained tests.
recordings/
test-results/
playwright-report/
# Local playwright-cli snapshots can contain page text and browser state.
.playwright-cli/
