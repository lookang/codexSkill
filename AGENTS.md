# AGENTS.md Instructions

Work proactively and carry tasks through to a verified outcome whenever access permits.

Use the exact paths, files, URLs, and parameters I provide.
Usually they are:

- `https://iwant2study.org` main index page
- `https://sg.iwant2study.org/ospsg/index.php/` joomla catalogue
- `https://iwant2study.org/lookangejss/` files to upload to server
- `https://github.com/lookang/` Github
- `https://weelookang.blogspot.sg/` Blog

Inspect the existing project before changing anything, and preserve unrelated work. For generated or repeated pages, identify and modify the canonical source or generator instead of patching individual outputs. Use loops to ensure the goals are achieved, instead of doing piece meal, do at system level.

When I ask to fix, build, upload, deploy, publish, or create something, treat that as authorization to complete the normal workflow without waiting for another confirmation. Run appropriate validation, tests, or syntax checks. For deployments, upload only the necessary changed files and verify the real live result with concrete evidence.

Prefer practical deliverables over general advice: edit the actual file, create the artifact, package the ZIP, provide copy-paste commands, or prepare the directly usable email. Clearly report:

- what changed;
- the exact files or URLs involved;
- how the result was verified;
- anything that remains blocked or requires my action.

This is a Windows PowerShell computer. Use PowerShell-compatible commands and avoid Unix-only syntax. Use apply_patch for careful file edits. Never overwrite, delete, reset, unpublish, or clean up unrelated or cloud-hosted content without explicit permission. Never guess credentials or expose secrets.

For browser-only work, use the existing authenticated browser session when possible. Preserve important URL parameters. If authentication is required, stop at the login boundary and ask me to log in. Learn and follow the real UI workflow rather than guessing fragile save or publishing steps.

For live websites, applications, and educational simulations, prioritize mobile usability, accessibility, pedagogical clarity, and preservation of existing functionality. Verify both the intended behavior and that no obvious regression was introduced.

Keep progress updates concise. Lead the final response with the outcome rather than a long account of the process.
