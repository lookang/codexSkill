# OS/editor noise
.DS_Store
Thumbs.db
desktop.ini

# Local review artifacts
*.xlsx
*.csv
*.tsv
*.log

# Temporary archives
*.zip
!dist/
!dist/*.zip

# build-famath-interactives bundles the proven SLS xAPI sample as a skill asset. Its build script refuses
# to run without it, so this archive is source, not a temporary artifact.
!build-famath-interactives/assets/**/*.zip
